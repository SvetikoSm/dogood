"use client";

/**
 * Раньше этот компонент при отсутствии основного CSS делал
 * window.location.reload(). На мобильном Safari и в Яндекс-браузере
 * перезагрузка рвала соединение и приводила к «зависанию» страницы.
 * Авто-перезагрузка убрана — компонент теперь безопасный no-op.
 */
export function SafariStylesRecovery() {
  return null;
}
